name: Deploy to Production Environment

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: deploy-prod

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Bun workspace
        uses: ./.github/actions/setup-bun-project

      # - name: Run database migrations
      #   run: |
      #     cd apps/backend
      #     bun run db:migrate
      #   env:
      #     DATABASE_URL: ${{ secrets.DATABASE_URL }}

      # - name: Deploy to Cloudflare Workers (Production)
      #   id: deploy-backend
      #   uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4
      #   with:
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     command: deploy --env prod
      #     workingDirectory: apps/backend
      #     secrets: |
      #       DATABASE_URL
      #       JWT_SECRET
      #     environment: prod
      #   env:
      #     DATABASE_URL: ${{ secrets.DATABASE_URL }}
      #     JWT_SECRET: ${{ secrets.JWT_SECRET }}

      # - name: Notify deployment
      #   env:
      #     DEPLOYMENT_URL: ${{ steps.deploy-backend.outputs.deployment-url }}
      #   run: |
      #     echo "✅ Backend deployed to production environment successfully!"
      #     echo "Worker: basic-knowledge-for-web-backend"
      #     echo "Deployment URL: ${DEPLOYMENT_URL}"

  deploy-frontend:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Bun workspace
        uses: ./.github/actions/setup-bun-project

      # - name: Build frontend
      #   run: |
      #     cd apps/frontend
      #     rm -f .env .env.local .env.production
      #     {
      #       echo "NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}"
      #       echo "JWT_SECRET=${JWT_SECRET}"
      #     } > .env.production
      #     bun run build:cloudflare
      #   env:
      #     NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
      #     JWT_SECRET: ${{ secrets.JWT_SECRET }}

      # - name: Build and deploy to Cloudflare Workers (Production)
      #   id: deploy-frontend
      #   uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4
      #   with:
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     command: deploy --env prod
      #     workingDirectory: apps/frontend
      #     secrets: |
      #       JWT_SECRET
      #     environment: prod
      #   env:
      #     NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
      #     JWT_SECRET: ${{ secrets.JWT_SECRET }}
      #     # Wrangler の OpenNext 自動検出を無効化し、`opennextjs-cloudflare deploy` 経由の
      #     # populate-cache（R2 疎通の一時失敗で deploy が落ちる）を回避する。
      #     OPEN_NEXT_DEPLOY: "true"

      # - name: Notify deployment
      #   env:
      #     DEPLOYMENT_URL: ${{ steps.deploy-frontend.outputs.deployment-url }}
      #   run: |
      #     echo "✅ Frontend deployed to production environment successfully!"
      #     echo "Worker: basic-knowledge-for-web-frontend"
      #     echo "Deployment URL: ${DEPLOYMENT_URL}"
